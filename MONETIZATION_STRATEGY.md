## 収益モデル仮説

このドキュメントは、初期要件ではなく将来の収益化候補を整理する。
初期開発では、写真からストーリーボードと生成素材を作る体験を優先し、収益化機能はドメイン境界と外部依存の設計余地として扱う。

## 基本方針

- 収益モデルは、初期から実装しすぎない。
- コイン制、広告収益、アフィリエイト、SaaS化を将来候補として扱う。
- APIコスト、生成待ち時間、外部サービス制限を前提にした設計にする。
- クラウドはGCPまたはAWSを想定するが、特定クラウドに直接依存しない。
- Clean Architectureにより、クラウド、決済、SNS、アフィリエイト、カレンダー、地図、キュー、通知はAdapterとして扱う。
- Domain/Application層は、GCP SDK、AWS SDK、Stripe SDK、SNS API SDK、Google API SDKに依存しない。

## 収益モデル候補

### 1. コイン制/SaaS課金

- ユーザーがコインを購入し、画像生成、動画生成、BGM生成、再生成、上位レシピ利用で消費する。
- 生成前に、概算コストまたは消費コイン数を表示する。
- 生成失敗時のコイン返却/非消費ルールを明確にする。
- Stripeなどの決済サービスは外部Adapterとして扱う。
- 初期DBではCoin/Credit履歴は実装対象外だが、将来的に追加できる余地を残す。

### 2. 広告収益/自動投稿

- 生成した動画をYouTube、TikTok、Instagramなどへ投稿し、広告収益や集客に使う。
- LLMにより、タイトル、説明文、ハッシュタグ、投稿文を構造化データとして生成する可能性がある。
- トレンド情報、BGM、尺、サムネイル、投稿時間のABテストを将来候補とする。
- SNS投稿は自動公開ではなく、初期は管理画面で確認してから公開するフローを優先する。
- SNS APIは外部Adapterとして扱い、Domain/Application層へSDK依存を入れない。

### 3. アフィリエイト/コンテクスト収益

- 旅行、場所、宿泊、体験、ガジェット、ファッションなどの文脈に応じたリンクを生成する。
- 旅行写真やジオタグ、画像解析結果から、場所や周辺情報を推定する可能性がある。
- 楽天トラベル、Agoda、Amazon、楽天市場などのアフィリエイト連携を将来候補とする。
- 空室状況、最安値、在庫などの変動情報は外部APIで扱い、Domainに直接持ち込まない。
- アフィリエイトリンク生成は、将来の別Bounded Context候補として扱う。

### 4. 旅行プラン作成

- 場所、日時、滞在時間、移動ルート、予定表をもとに旅行プランを作る機能を将来候補とする。
- Google Calendarなどのカレンダー連携を検討対象に含める。
- Google Mapsなどの地図/ルート連携を検討対象に含める。
- 写真/動画生成ドメインに直接混ぜず、将来のTravel Planning Contextとして分離する可能性を残す。

### 5. エージェント/スキル販売

- 「プロ志向のエモさ」「ノスタルジックなエモさ」など、生成レシピをスキルとして扱う。
- スキルは、プロンプト、モデル設定、参照画像方針、色味、尺、BGM方針などのセットとして定義する可能性がある。
- 高度なスキルや追加生成には、より多くのコインを消費する設計を検討する。
- 初期はStylePresetやGenerationRequestの拡張として扱い、必要になった時点でSkill Contextとして分離する。

## クラウド非依存方針

GCPまたはAWSを利用する可能性は高いが、初期設計ではクラウド固有サービスをApplication/Domain層に露出させない。

想定するPort:

- `ObjectStoragePort`
    - 元写真、生成画像、動画、BGM、書き出しファイルを保存/取得する。
    - Adapter候補: Local File System、GCS、S3。
- `JobQueuePort`
    - 画像生成、動画生成、BGM生成、SNS投稿、メタデータ生成などの非同期ジョブを投入する。
    - Adapter候補: In-memory queue、Cloud Tasks/Pub/Sub、SQS。
- `ProgressEventPort`
    - 生成中の進捗をUIへ通知する。
    - Adapter候補: Local event emitter、SSE、WebSocket、Pub/Sub bridge。
- `SchedulerPort`
    - 定期投稿、トレンド収集、期限切れデータ削除、バックアップを実行する。
    - Adapter候補: cron、Cloud Scheduler、EventBridge。
- `PaymentPort`
    - コイン購入、決済結果、返金、請求履歴を扱う。
    - Adapter候補: Stripe、その他決済サービス。
- `SocialPublishPort`
    - SNS投稿、投稿状態確認、投稿結果取得を扱う。
    - Adapter候補: YouTube、TikTok、Instagramなど。
- `AffiliateLinkPort`
    - 宿泊、商品、体験、ガジェットなどのリンク生成を扱う。
- `CalendarPort`
    - 旅行予定や制作予定を外部カレンダーへ同期する。
- `RoutePlanningPort`
    - 場所、移動時間、ルート候補を取得する。

## 非同期生成と進捗通知

- 動画生成やBGM生成は時間がかかるため、同期HTTPだけで完結させない。
- 初期はローカル実装でも、UseCase上はジョブ投入、状態更新、進捗通知に分ける。
- UIでは、解析中、画像生成中、動画生成中、レンダリング中、完了、失敗などの状態を表示できるようにする。
- SSEは初期候補として有力だが、Application層では `ProgressEventPort` 経由にする。
- コイン消費タイミングは、ジョブ投入時、外部API成功時、成果物採用時のどれにするかを後続で決める。

## 初期管理画面の将来候補

- 自分専用の管理画面から、写真をアップロードして複数SNS向け動画を生成する。
- 生成結果を確認し、公開ボタンでSNS投稿や素材配信を行う。
- 最初から完全自動投稿にはせず、レビュー工程を残す。
- 反応データを収集し、次回の生成プロンプトや投稿メタデータに反映する可能性を残す。

## リスクと注意点

- 外部API料金、モデル料金、動画生成時間は変動するため、価格表をDomainに固定しない。
- SNS APIやアフィリエイトAPIは規約変更があり得るため、ProviderごとのAdapterに閉じる。
- スクレイピングは規約/法務リスクがあるため、可能な限り公式APIや許可されたデータ取得手段を優先する。
- ユーザー写真、位置情報、旅行予定、カレンダー予定はプライバシー性が高いため、同意、保存期間、削除、共有範囲を明確にする。
- 旅行や宿泊の提案は、価格/空室/営業時間などの最新性が必要なため、表示時点で再取得する設計を検討する。

## 完了条件

- 収益モデル候補を記録する。
- コイン制、広告、自動投稿、アフィリエイト、旅行プラン、スキル販売の位置づけを記録する。
- GCP/AWSに依存しないクラウド抽象化方針を記録する。
- 初期スコープに入れない項目と、将来拡張として残す項目を分ける。
