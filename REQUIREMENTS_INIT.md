## プロジェクト概要
- [x] 実際の写真をベースにエモい（Emotionalな）、シネマティックなアニメーション・動画を作成する。
    - 感情設計
        - ユーザーが届けたい感情を選択できるようにする。
        - 添付された実際の写真をベースに、AIが適切な感情候補を提案できるようにする。
        - 基本方針として、AIが写真から感情候補を提案し、ユーザーが選択・調整する流れを想定する。
        - AIによる写真解析では、以下の観点を読み取る。
            - 写っている人物の関係性。
            - 表情。
            - 年齢や成長の変化。
            - 場所や季節。
            - 写真の年代感。
            - イベント性。
                - 誕生日。
                - 結婚式。
                - 旅行。
                - 卒業。
                - その他の記念日や人生イベント。
            - 写真全体の雰囲気。
            - 複数写真を並べたときのストーリー性。
    - プロジェクト管理
        - `projects` データベースで、各プロジェクトごとにアップロードされた写真や届けたい感情を管理する。
    - 写真アップロード条件
        - 1プロジェクトあたりの写真枚数は、初期値として最大20枚を想定する。
        - 最大枚数は将来的に変更される可能性がある。
        - 初期対応する写真形式
            - JPG / JPEG。
            - PNG。
            - HEIC。
            - WebP。
        - 写真ごとの任意メモ
            - ユーザーが写真ごとに任意でメモを入力できるようにする。
            - メモには、写真の説明、写っている人、撮影時期、場所、思い出、表現したいこと、使いたくない表現などを入力できる想定とする。
            - AIが写真だけでは判断できない文脈を補う情報として扱う。
        - 写真ごとの利用状態
            - 写真ごとに利用状態を設定できるようにする。
            - 利用状態は以下の3種類とする。
                - 使用。
                - 未使用。
                - 参考のみ。
            - 参考のみの写真は、ストーリーボードのシーンには直接使わない。
            - 参考のみの写真は、関係性、雰囲気、文脈、一貫性の参考としてAIが利用する。
        - 写真の並び替え
            - ドラッグ&ドロップで手動並び替えできるようにする。
            - アップロード順で扱えるようにする。
            - AIおすすめ順をオプションとして利用できるようにする。
            - 撮影日時順は、初期段階では不要とする。
    - 人物・顔の扱い
        - 本人らしさはできるだけ維持する。
        - 完全な顔再現よりも、エモいシネマティック表現を優先する。
        - 最終的な人物表現は、ユーザーが選択する画像スタイルにも左右される。
    - 画像スタイル選択
        - 著作権上の懸念を避けるため、特定の既存作家名やスタジオ名を直接的なスタイル名として定義しない。
        - 一般的な画像スタイルをユーザーが選択できるようにする。
        - 画像スタイルにはプレビューを用意する。
            - プレビュー画像はこちら側で用意した例を使用する。
            - 同じ画像をそれぞれのスタイルで表現した例を並べ、ユーザーが比較しやすいようにする。
            - ユーザーが完成イメージを容易に想像できるようにする。
        - 初期候補として、以下の一般的な画像スタイルを用意する。
            - シネマティック実写風。
            - アニメ映画風。
            - 温かい手描き風。
            - 透明感のある光表現。
            - フィルム写真風。
            - 水彩イラスト風。
            - モノクロ映画風。
            - 立体感のある3Dアニメ風。
            - AIにおまかせ。
        - ユーザーがアップロードした1枚の画像をもとに、カスタムスタイルのプロンプトを作成できるようにする。
        - 作成されたカスタムスタイルデータは、ユーザーが削除しない限り保存する。
        - カスタムスタイルの利用範囲
            - 初期段階では、同じユーザーの全プロジェクトで使えるようにする。
            - 将来的には、チームや共有ユーザーにも使えるようにする。
            - 将来的には、共有スタイルライブラリにできるようにする。
        - スタイルの適用単位
            - 基本的に、画像スタイルはプロジェクト単位で適用する。
            - 基本的に、シーンごとに別のスタイルは選択しない。
            - 各シーン画像は、同時に生成されることを想定する。
        - プロジェクト共通プロンプト
            - プロジェクトごとに、ベースとなる共通プロンプトを定義する。
            - 共通プロンプトは、ユーザーの意向、感情設計、選択した画像スタイルなどをもとに生成する。
            - 共通プロンプトだけでは画像の一貫性を担保できない可能性があるため、一貫性確認の仕組みを検討する。
        - テスト生成
            - 本生成前に、1枚または数枚の画像をテスト生成できるようにする。
            - 初回のテスト生成では、3パターン程度の画像を生成する。
            - テスト生成により、画像スタイル、人物表現、色味、雰囲気の一貫性を確認する。
            - テスト生成の具体的な運用は、画像生成モデルのベストプラクティスに従う。
        - テスト生成後の調整
            - 初心者向けに、直接プロンプト編集ではなく、分かりやすい調整ボタンやスライダーを中心にする。
            - 調整例
                - もっと暖かく。
                - もっと映画っぽく。
                - もっと本人らしく。
                - もっとアニメ風に。
                - もっと自然に。
                - もう少し淡く。
            - 内部的には、ユーザーの調整内容をもとにプロジェクト共通プロンプトを更新する。
        - テスト生成の確定フロー
            - ユーザーはテスト生成画像の中から1枚を選んで確定する。
            - さらにテスト生成したい場合は、専用のボタンから追加生成できるようにする。
            - 確定後に、全シーン画像を一括生成する。
            - 確定後も再テスト・再生成できるようにする。
            - 再テスト・再生成を行う場合でも、前回のバージョンに戻れるようにすることを必須とする。
        - バージョン管理
            - テスト生成時点の設定を保存する。
            - 確定したテスト画像を保存する。
            - 本生成された全シーン画像のセットを保存する。
        - 本生成画像の扱い
            - 各シーンで、元写真と生成画像を比較できるようにする。
            - 各シーン画像の初回生成は、基本的に1枚のみ生成する。
            - シーンごとに生成画像を再生成できるようにする。
            - シーンごとに生成画像の採用・未採用を判断できるようにする。
            - シーンごとの再生成では、同じ条件で再生成できるようにする。
            - シーンごとの再生成では、シーン単位の設定を変更して再生成できるようにする。
            - 元写真の差し替えは初期必須とはせず、必要になった場合に検討する。
            - 再生成によって未採用になった画像は、ユーザーが削除するまで履歴として残す。
            - フェーズ2以降に渡すデータでは、基本的に採用された生成画像だけを対象にする。
            - 未採用画像の履歴は、デバッグや復元用としてDBに保持する。
    - フェーズ1の完了条件
        - 写真を解析して、感情候補を出す。
        - ユーザーが感情を選ぶ。
        - 写真ごとにシーン説明を作る。
        - シネマティックな画像生成プロンプトを作る。
        - ストーリーボードを作る。
    - ストーリーボード構成
        - 写真順は基本的にユーザーが決定する。
        - 必要に応じて、AIが写真群からおすすめの順番を提案できるようにする。
        - AIによる写真順の提案を使用する場合は、写真解析開始前にユーザーがToggleなどで明示的にオンにするUIを想定する。
        - Toggleの対象は、写真の順序提案にAIを使うかどうかとする。
        - AIによる感情候補、シーン構成、シーン説明、画像生成プロンプトの提案は通常フローとして行う。
        - AIが写真群からおすすめのシーン構成を提案する。
        - ユーザーが写真の順番を並べ替えられるようにする。
        - ユーザーがシーン構成を調整できるようにする。
    - ストーリーボードの1シーンに含める情報
        - 使用する元写真。
        - シーンタイトル。
        - シーン説明。
        - 届けたい感情。
        - 画像生成プロンプト。
        - 画角・カメラワーク。
        - 色味・ライティング。
        - アニメーション化するときの動きの方向性。
        - ユーザー向けの編集メモ。
        - 実際の写真を使わずAI生成のみで補完するシーンの場合は、どのシーン同士のつなぎになるのかを明示する。
    - シーン編集
        - 基本的に、ストーリーボード内の各項目はすべてユーザーが変更できるようにする。
        - アニメーション初心者でも扱いやすいように、専門性が高い項目は選択式を基本にする。
            - 画角・カメラワーク。
            - 色味・ライティング。
            - アニメーション化するときの動きの方向性。
        - 必要に応じて、選択式に加えて自由入力でも補足できるようにする。
        - 選択式のラベル
            - 初心者向けに分かりやすいラベルを用意する。
            - 表示言語はアプリケーション設定画面でユーザーが選択した言語に従う。
            - 内部的な選択肢の意味と、ユーザーに表示するラベルは分けて管理する。
    - 言語設定
        - 一般的なアプリケーションと同様に、基本的にすべての表示・生成内容を言語設定の対象にする。
            - アプリのUI表示。
            - 選択式ラベル。
            - AIが生成するシーンタイトル。
            - AIが生成するシーン説明。
            - AIが生成する画像生成プロンプト。
            - ユーザー向け編集メモ。
            - エクスポートされるストーリーボード。
        - 初期対応言語は日本語と英語のみとする。
        - 将来的に対応言語を拡張できる設計にする。
        - 画像生成モデルに渡す内部プロンプトと、ユーザーに表示するプロンプト文言は必要に応じて分けられるようにする。
    - AI補完シーン
        - 実際の写真から作成されたストーリーボード上で、シーンとシーンの間に追加できるようにする。
        - シーン間をHoverすると表示される「プラス」操作から、AI補完シーンを生成できるようにする。
        - AI補完シーンの追加は、基本的にユーザーの判断で行われる。
        - AIが自動的に補完シーンを追加するのではなく、ユーザーの明示的な操作を起点にする。
        - 追加時の入力フロー
            - ユーザーが補完シーンの用途を選ぶ。
            - ユーザーが任意で自由入力できる。
            - AIが1〜3案を提案する。
    - 後続フェーズとの関係
        - フェーズ2以降は、フェーズ1でストーリーボードが完成していることを前提とする。
        - CapCutなどで編集しやすい構成案を出す工程は、ストーリーボード完成後に扱う。
        - ストーリーボード作成と動画編集向け構成案の作成は、基本的に同時には扱わない。
    - ストーリーボードの閲覧・出力形式
        - アプリ内でカード形式で閲覧できるようにする。
        - アプリ内でタイムライン形式で閲覧できるようにする。
        - アプリ内で一覧表形式で閲覧できるようにする。
        - 元写真と生成画像を比較できるようにする。
        - 元写真のみ、生成画像のみなどのフィルタリング表示ができるようにする。
        - JSONで出力できるようにする。
        - 将来の動画生成用に構造化データとして保存する。
        - 現時点では、Markdown出力やPDF出力は不要とする。
    - 保存と完成状態
        - 完成・不完成に関わらず、ストーリーボードの作成状態はDBに保存する。
        - ユーザーが途中から作業を再開できるようにする。
        - 完成後もストーリーボードを編集できるようにする。
        - ストーリーボードの状態は以下を想定する。
            - 下書き。
            - 生成中。
            - 生成失敗。
            - 編集中。
            - 完成。
        - 生成失敗時の復旧操作
            - 失敗理由を表示する。
            - 失敗したシーンだけ再生成できるようにする。
            - 条件を編集して再生成できるようにする。
    - 
    - 
- [x] 画像生成には、デフォルトとしてOpenAI gpt-image-2を使用する（Clean Architectureによって、画像生成は外部に依存しないようにすることで、将来の拡張性を担保し、対応できるようにすること）。
    - デフォルトモデル
        - 初期のデフォルト画像生成モデルは、OpenAI `gpt-image-2` とする。
        - 旧記載の「OpenAI Imagen2.0」は使用せず、OpenAI APIの現行モデル名に合わせる。
    - 初期対応プロバイダー
        - 初期実装ではOpenAIのみを画像生成プロバイダーとして扱う。
        - ただし、将来の `SeedanceImageAdapter` や別プロバイダー追加に備える。
        - ユーザー体験上は、テスト生成前のステップでモデルを選択できるようにする。
        - モデル選択UIでは、`gpt-image-2` のような実モデル名も表示する。
        - 将来的に複数プロバイダーを扱う場合は、プロバイダーごとにグループ分けし、実モデルを表示・選択可能にする。
    - 必要な画像生成操作
        - テキストプロンプトから画像生成できるようにする。
        - 元写真を入力として画像生成・編集できるようにする。
        - 複数画像を参照して一貫性を保てるようにする。
        - 一括生成時に、画像サイズ・品質・形式を指定できるようにする。
        - 画像サイズ・品質・形式の指定は、初心者向けプリセットを基本にする。
        - 初期プリセットは「標準」とする。
        - 生成結果を保存する。
        - 失敗時に再試行できるようにする。
    - モデル選択権限
        - モデル選択は、すべてのユーザーが変更できるようにする。
    - 生成結果の保存
        - 生成結果は、ユーザーがいつでも確認できるように保存する。
        - 初期の保存方式は、上記を満たせるなら特定の実装に固定しない。
        - 将来的には、S3、R2、GCSなどのオブジェクトストレージを利用できるようにする。
        - Clean Architectureによって、保存先も差し替え可能にする。
    - 画像削除
        - 元写真や生成画像は、ユーザーが削除できるようにする。
        - プロジェクト削除時には、関連する元写真や生成画像もまとめて削除対象にする。
        - 削除後は復元期間を設ける。
        - 復元期間は基本的に7日以内とし、それ以降は完全に削除する。
    - 画像生成リクエスト履歴
        - 画像生成リクエストの履歴として、以下を保存する。
            - 使用モデル。
            - プロンプト。
            - 元写真ID。
            - 参照画像ID。
            - サイズ・品質・形式。
            - 成功/失敗。
            - エラー理由。
            - 生成日時。
            - コスト/コイン消費量。
        - ユーザーには生成履歴として簡易表示する。
        - 画像生成のバージョン履歴と紐づけて、リクエスト内容を履歴に残す。
    - APIキー管理
        - 初期実装では、システム側のOpenAI APIキーを使用する。
        - 将来的にコイン制でユーザーが生成コストを支払う想定のため、ユーザー自身のAPIキー登録は初期要件に含めない。
        - 将来的には、コイン制だけでなく、各ユーザーのCodexプランを利用して画像生成や文字生成を行える選択肢も検討する。
        - Codexプラン利用時は、ユーザーがコインを購入して生成ごとに消費する必要がない体験を目指す。
    - 将来差し替え可能にする範囲
        - モデル名を差し替え可能にする。
        - APIプロバイダーを差し替え可能にする。
        - 画像生成プロバイダーやモデルは、シーンごとには変更しない。
        - コスト計算は、API料金をシステム側で入力・管理する想定とする。
        - 将来的には、1枚ごとの金額または専用コイン消費量を生成前の見積もりとして表示できるようにする。
        - 将来的には、ユーザーがお金を支払ってコインを購入し、そのコインを使って画像生成できるようにする。
    - アーキテクチャ方針
        - 画像生成の失敗や制限をどの層で扱うかは、Clean Architectureのベストプラクティスに従う。
        - Domain層は、画像生成リクエスト、生成結果、生成状態などの概念を扱う。
        - UseCase層は、テスト生成、一括生成、シーン単位再生成、再試行などの生成フローを制御する。
        - Adapter層は、OpenAI API固有のモデル名、入力形式、レスポンス形式、エラーを吸収する。
        - UI層は、ユーザー向けの失敗理由、再試行操作、モデル選択を表示する。
        - 技術的な詳細を除き、画像生成の制限やエラー内容は基本的にユーザーへ表示する。
        - 技術的な詳細は、開発者ログや内部ログで確認できるようにする。
    - ユーザー体験の流れ
        - 詳細は `USER_EXPERIENCE_FLOW.md` に記録する。
    - 簡易ポート/アダプター案
        - `ImageGenerationPort`
            - 画像生成・画像編集・再生成を行うための抽象インターフェース。
        - `ImageStoragePort`
            - 生成画像や元写真を保存・取得するための抽象インターフェース。
        - `ImageGenerationProvider`
            - 利用可能な画像生成プロバイダーやモデル情報を扱う抽象インターフェース。
        - `OpenAIImageGenerationAdapter`
            - OpenAI `gpt-image-2` などのAPI呼び出しを担当する実装。
        - `ImageGenerationUseCase`
            - テスト生成、一括生成、シーン単位再生成、失敗時の再試行を制御するユースケース。
- [x] フェーズ別ロードマップとして、第一フェーズでは動画生成に渡せる状態のストーリーボードと採用画像セットを作成し、第二フェーズではストーリーボードをベースに動画を作成し、第三フェーズではCodexやClaude Code EditorなどのAIエディタを使ってストーリーボード修正や動画作成フローを自動化できるようにする。
    - フェーズ統合方針
        - 第一フェーズ、第二フェーズ、第三フェーズは、個別項目ではなく1つのロードマップ項目として管理する。
        - 各フェーズの成果物、開始条件、完了条件を明確に分ける。
    - フェーズ1
        - 最終成果物は、動画生成に渡せる状態のストーリーボードと採用画像セットとする。
        - この時点では、基本的に動画生成は行わない。
        - 実際の写真をベースに画像生成を行う。
        - 必要に応じて、ストーリーボードが自然につながるようにAI補完画像を作成する。
        - CapCutなどの動画編集ツールやフェーズ2の動画生成に渡せる構造化データを作成する。
    - フェーズ2
        - フェーズ1で完成したストーリーボードと採用画像セットを前提にする。
        - ストーリーボードをもとに動画生成プロンプトを作成する。
        - 採用画像から短い動画クリップを生成する。
        - シーンごとの動画を生成する。
        - シーンごとの動画クリップ尺は変更可能にする。
        - 動画クリップ尺の指定可能範囲は、動画生成モデルの仕様に従う。
        - 初期値は1シーンあたり5秒程度とする。
        - 1本の動画全体の尺は、写真枚数とシーン数から自動計算する。
        - BGMのみ生成する。
            - BGM生成にはSuno AIを使用する想定とする。
            - 初期は歌詞なしのBGMのみとする。
            - 将来的には歌ありも検討する。
            - ユーザーがBGMのみ/歌ありを選択可能にすることを将来要件として想定する。
            - BGMは、基本的に動画全体に対する1本として扱う。
        - CapCutなどに渡せる素材一式を作成する。
            - 生成動画クリップ。
            - 採用画像/採用動画。
            - 動画全体のBGM。
            - 字幕テキスト。
                - 字幕は将来対応とする。
            - 編集メモ。
        - アプリ内で1本の動画として書き出せるようにする。
        - 初期の動画書き出し形式はMP4のみとする。
        - アプリ内書き出し時の編集機能
            - トランジション選択を検討する。
            - OSSや既存ライブラリを調査し、簡単に実現できる場合のみ選択可能にする。
            - CanvaやCapCutの方が編集機能は豊富であるため、トランジションや高度な編集機能で競争しない。
        - 動画生成リクエスト履歴
            - 動画生成リクエストの履歴保存は必須とする。
            - 保存する項目
                - 使用モデル。
                - 動画プロンプト。
                - 元画像ID。
                - シーンID。
                - 尺。
                - 成功/失敗。
                - エラー理由。
                - 生成日時。
                - コスト/コイン消費量。
    - フェーズ2の動画生成アーキテクチャ
        - 動画生成モデルは、初期候補としてSeedance 2.0を想定する。
        - Clean Architectureにより、動画生成プロバイダーも差し替え可能にする。
        - 簡易ポート/アダプター案
            - `VideoGenerationPort`
                - 動画生成、動画再生成、動画生成状態の取得を行うための抽象インターフェース。
            - `SeedanceVideoGenerationAdapter`
                - Seedance 2.0などの動画生成API呼び出しを担当する実装。
            - 将来の別動画生成プロバイダー用Adapter
                - 将来追加する動画生成サービスを同じUseCaseから扱えるようにする。
    - フェーズ3
        - CodexやClaude Code EditorなどのAIエディタを使用して、ストーリーボード修正や動画作成フローを自動化できるようにする。
        - 将来的には、ユーザーがUIで行う `USER_EXPERIENCE_FLOW.md` の作業を、Codexとのチャット内で完結できるようにする。
        - Codexとのチャットから、画像生成、文字生成、ストーリーボード修正、動画生成フローの実行指示を行えるようにする。
        - 初期段階では、一般ユーザー向け機能ではなく、開発者側のローカル利用機能として提供する想定とする。
        - まずは自分のみがローカルで使える機能として扱う。
        - 構造化されたストーリーボードを、AIエディタが安全に編集できる形式で保存する。
        - 初期に自動化したい範囲
            - ストーリーボードJSONの修正。
            - 画像生成プロンプトの一括調整。
            - フェーズ2用動画プロンプト生成。
    - フェーズ4以降の将来拡張候補
        - 旅行プランの作成機能を将来候補として残す。
        - 旅行プランでは、場所、日時、滞在時間、移動ルート、予定表を扱う可能性がある。
        - Google Calendarなどの外部カレンダー連携を検討対象に含める。
        - Google Mapsなどの地図/ルート系サービス連携を検討対象に含める。
        - 初期スコープには含めず、まずは写真からストーリーボードと生成素材を作る体験を優先する。
        - 将来の旅行プラン機能に備えて、写真やプロジェクトには場所、日時、イベント文脈などのメタデータを拡張できる余地を残す。
        - 旅行プラン作成は、現時点では動画生成ドメインに直接混ぜず、将来の別Bounded Context候補として扱う。
- [x] 収益モデルは、初期要件には含めすぎず、将来拡張候補として整理する。
    - 詳細は `MONETIZATION_STRATEGY.md` に記録する。
    - 将来候補として、コイン制、広告収益、自動投稿、アフィリエイト、旅行プラン作成、エージェント/スキル販売を扱う。
    - 初期開発では、写真からストーリーボードと生成素材を作る体験を優先する。
    - 収益化機能は、APIコスト、生成待ち時間、外部サービス制限、プライバシーリスクを踏まえて後続で詳細化する。
    - コイン/決済、SNS投稿、アフィリエイト、旅行プラン、カレンダー連携、地図/ルート連携は、将来の別Bounded Context候補として扱う。
- [x] 基本的なアーキテクチャは、シンプルなHexagonal Architecture ＆ Clean Architecture & Domain Driven Designで行う。レポジトリ構成、使用言語、その他諸々は議論の必要あり。
    - 基本方針
        - 旧記載の `Jexagonal Architecture` は、`Hexagonal Architecture` の誤記として扱う。
        - シンプルなHexagonal Architecture、Clean Architecture、Domain Driven Designを組み合わせる。
        - 外部サービスやフレームワークに依存しすぎない構成にする。
        - ただし、初期実装では過度に複雑な抽象化を避ける。
    - 使用言語
        - 初期実装の言語は、要件と実装容易性に応じて選択する。
        - 初期バックエンドはTypeScriptを第一候補とする。
        - Goは将来的な選択肢として残すが、初期実装ではTypeScriptに寄せる。
        - 特定サービスがPython系SDKしか提供していない場合は、Pythonの使用も可能とする。
        - 複数言語を採用する場合は、複雑性が増えるため、境界と責務を明確にする。
    - 初期アプリ形態
        - 初期アプリは、ローカルで動くWebアプリとする。
        - フロントエンドはNext.jsを使用する。
        - 初期構成は、フロントエンドとバックエンドAPIを分ける。
        - ただし、UIモック段階ではNext.jsのみで作成してよい。
        - 将来的にデスクトップアプリ、モバイルアプリへ拡張可能なアーキテクチャにする。
        - 将来拡張を見据えるが、初期段階ではローカルWebアプリとして成立することを優先する。
    - クラウド方針
        - 本番クラウドはGCPまたはAWSを候補とする。
        - ただし、Application/Domain層は特定クラウドのSDKやサービス名に依存しない。
        - クラウド実行基盤、オブジェクトストレージ、ジョブキュー、スケジューラー、進捗通知、ログ/監視はAdapterとして扱う。
        - ローカル実装、GCP実装、AWS実装をPort経由で差し替えられるようにする。
        - 初期実装ではローカルで完結するAdapterを優先し、本番化時にGCP/AWS Adapterを追加する。
    - レポジトリ方針
        - 初期はモノレポ構成を基本とする。
        - 初期モノレポは、将来のrepo splitを前提にした小さなモノレポとして扱う。
        - 将来的にiOS、Android、Webアプリ、デスクトップアプリへ拡張する際、既存コードを大幅に改変しないで分離・拡張できることを重視する。
        - モノレポを継続するか、プラットフォームごとに別リポジトリ化するかは、採用技術とチーム体制に応じて後で判断する。
        - すべてのプラットフォームを同一モノレポに永久固定する前提にはしない。
        - `apps/*` は `packages/*` に依存してよいが、`packages/*` は `apps/*` に依存しない。
        - `packages/shared` はAPI DTO、API schema、共通定数、エラーコードなどに限定する。
        - `packages/shared` にReact component、Next.js依存コード、DB model、WorkOS user object、OpenAI SDK response、UI状態管理を入れない。
        - UIコンポーネントやプラットフォーム固有実装を無理に共有しない。
        - 複数言語にまたがり複雑性が増す場合は、複数リポジトリへの分割も想定する。
        - モノレポ/複数リポジトリの詳細な非機能要件は、後続の議論で決定する。
        - 初期のパッケージ構成案
            - `apps/web`
                - Next.jsによるフロントエンド。
                - UIモック段階では、このアプリのみで進めてよい。
            - `apps/api`
                - TypeScriptによるバックエンドAPI。
                - 初期はInfrastructure実装を `apps/api` 内に置いてもよい。
            - `packages/domain`
                - Entity、Value Object、Domain Service、Domain Event、不変条件を扱う。
            - `packages/application`
                - UseCase、Input Port、Output Port、Application Serviceを扱う。
            - `packages/infrastructure`
                - DB、外部API、ストレージ、認証などのAdapter実装を扱う。
                - 初期段階では必須分割ではなく、複雑性が増えた時点で `apps/api` から切り出してよい。
            - `packages/shared`
                - フロントエンドとバックエンドで共有する型、スキーマ、定数を扱う。
    - DDD方針
        - DDDの主要ドメインや集約境界は、ベストプラクティスに従って決定する。
        - 初期段階では、実装前に過剰に細かい集約へ分割しすぎない。
        - Project、Photo、Storyboard、Scene、GeneratedImage、Style、GenerationRequest、Userなどの概念を候補として扱う。
        - 実際の集約境界は、状態遷移、ライフサイクル、不変条件、ユースケースの責務をもとに決める。
        - 拡張性を担保できるなら、具体的な集約分割はベストプラクティスに従って決める。
        - 初期案としては、Projectを中心にしつつ、Storyboard、Scene、PhotoAsset、GenerationJob、StylePresetなどを必要に応じて分ける。
        - `domain` 層は、外部ライブラリ非依存を原則にする。
        - `domain` 層は、DB ORM、HTTP、OpenAI SDK、Next.js、Zodなどに依存しない。
        - Domainの不変条件は、Domain内で管理する。
    - API設計
        - 初期APIはREST中心とする。
        - ZodやOpenAPIなどを用いてスキーマを管理する。
        - フロントエンドとバックエンド間の契約を明確にし、将来のクライアント追加に備える。
        - API入力のバリデーションはZodを基本とする。
        - API入力検証とDomain不変条件は分けて管理する。
    - 認証設計
        - 初期からWorkOSを採用することは可能とする。
        - 認証はApplicationの外側にあるAdapterとして扱う。
        - ローカル自動テストでは、WorkOSを完全にバイパスできる設計にする。
        - `userId` と `organizationId` の内部モデルを先に持つ。
        - `local/test auth adapter` を先に作る。
        - `WorkOS adapter` は後から差し替え可能にする。
        - WorkOSの実ログインを伴うE2Eテストは、少数のsmoke testに限定する。
    - 差し替え可能にする外部依存
        - DB。
        - 画像生成。
        - 動画生成。
        - ストレージ。
        - 認証。
        - 決済/コイン。
        - BGM生成。
        - カレンダー連携。
        - 地図/ルート連携。
        - クラウド実行基盤。
        - ジョブキュー。
        - スケジューラー。
        - 進捗通知。
        - ログ/監視。
    - 完了条件
        - 初期アーキテクチャ方針を記録する。
        - 使用言語を記録する。
        - 初期アプリ形態を記録する。
        - クラウド非依存方針を記録する。
        - モノレポ方針を記録する。
        - DDD方針を記録する。
        - 差し替え対象を記録する。
        - API設計を記録する。
        - 認証Adapter方針を記録する。
        - 簡易レポジトリ構成案を記録する。
- [x] UIが未決定なので、先に簡易的なモックを作成してから、細かなDB設計やUI設計を行なっていく。モックの作成には、Typescriptを使用予定であるが、フロントエンド、モバイルアプリ、Webアプリ、デスクトップアプリなど、様々なプラットフォームで使用できるフレームワークであり、将来の拡張性を担保し、対応できるようにすること）。
    - UIモックの目的
        - 初期モックでは、画面遷移とUX確認を優先する。
        - ユーザーが迷わず素材を入れて、感情や構成を選び、ストーリー/画像生成まで進めるかを確認する。
        - 実装に近いプロトタイプは、主要フローが固まってから作成する。
    - 初期モックに含める画面
        - プロジェクト一覧。
        - プロジェクト作成。
        - 写真アップロード。
        - 写真管理。
        - 感情選択。
        - ストーリーボード編集。
        - 生成画像比較。
    - データ方針
        - 初期モックでは、実際のAI生成やDB保存は行わない。
        - 初期モックはすべてモックデータで構成する。
    - 技術スタック
        - Next.js、TypeScript、Tailwind CSSを使用する。
        - Next.jsは、画面遷移と将来実装への接続を見据えて使用する。
        - TypeScriptは、状態やモックデータの型安全性を担保するために使用する。
        - Tailwind CSSは、UI調整を素早く行うために使用する。
    - UIコンポーネント
        - shadcn/uiを使用する。
        - 初期導入は必要最小限にする。
        - 初期導入するコンポーネント
            - Button。
            - Input。
            - Textarea。
            - Select。
            - Tabs。
            - Dialog。
            - Dropdown Menu。
            - Badge。
            - Card。
            - Progress。
            - Separator。
        - データテーブル、Command、複雑なフォーム系コンポーネントは初期では不要とする。
        - モックでも操作感を確認したいため、ボタン、タブ、ダイアログ、セレクトなどのアクセシビリティや状態管理はshadcn/uiに寄せる。
    - 画面遷移
        - UIモックでは、クリック可能な画面遷移まで作成する。
        - サイドバー、ステップナビ、主要ボタンなどから各画面へ移動できるようにする。
        - フェーズ1の流れを、モックデータで最後まで確認できるようにする。
    - 写真アップロードモック
        - 実ファイル選択に対応する。
        - 選択した画像のローカルプレビューを表示できるようにする。
        - 初期モックでは、実アップロードやDB保存は行わない。
    - モック画像
        - ユーザーが画像を選ばなくても一通り確認できるようにする。
        - 初期表示では、プレースホルダーまたはサンプル画像を使用する。
        - アップロード画面では、ユーザーが選択したローカル画像をプレビューする。
        - 比較画面では、固定の生成結果サンプル画像を使用する。
        - サンプル画像の配置例
            - `public/mock/sample-family-01.jpg`
            - `public/mock/sample-family-02.jpg`
            - `public/mock/generated-01.jpg`
            - `public/mock/generated-02.jpg`
    - ストーリーボード編集モック
        - 初期モックでは、ドラッグ&ドロップ並び替えは不要とする。
        - ストーリーボードの見た目を確認できるようにする。
        - シーンの上下移動ボタンで、並び順の調整イメージを確認できるようにする。
    - 生成画像比較モック
        - 元写真と生成画像の左右比較を優先する。
        - 採用/未採用の切り替え操作を配置する。
        - 再生成ボタンの配置を確認できるようにする。
    - デザイン方向性
        - 全体としては、Canva + CapCut寄りの体験を参考にする。
        - Canvaからは、素材を選ぶ、見比べる、完成物を作る感覚を参考にする。
        - CapCutからは、タイムライン、ストーリーボード編集、生成結果比較の体験を参考にする。
        - Notionからは、プロジェクト整理やメタ情報管理のみ参考にする。
        - Linearからは、設定、ジョブ状態、履歴管理の落ち着いたUIのみ参考にする。
        - ただし、作業UIとしては落ち着いた見た目にする。
    - レスポンシブ方針
        - 初期はデスクトップ優先とする。
        - 最低限、タブレット幅まで崩れないようにする。
        - スマホ対応は、将来的な閲覧・簡易確認を想定する。
        - 初期段階では、スマホでの本格編集は必須としない。
    - 多言語対応
        - 初期モックでは、多言語切り替えUIは入れない。
        - 将来の多言語対応を邪魔しない程度に留める。
    - 完了条件
        - 主要7画面を行き来できる。
        - フェーズ1の一連の流れをモックデータで最後まで確認できる。
- [x] データベースは、ローカルではSQLiteを使用。プロダクト環境時のDBは未決定（Clean Architectureによって、DBは外部に依存しないようにすることで、将来柔軟に対応できるようにすること）。
    - 基本方針
        - ローカルDBはSQLiteを使用する。
        - 本番DBは現時点では確定しない。
        - 本番DBの第一候補はPostgreSQLとする。
        - Clean Architectureによって、DB実装はRepository Adapterとして扱う。
        - Domain層はDB、ORM、クエリビルダに依存しない。
    - ORM/クエリビルダ
        - TypeScript実装ではDrizzleを使用する。
        - Drizzleの薄いORMとしての性質が、初期プロダクトに合うため採用する。
        - Drizzleは `apps/api` のRepository Adapter内だけで使用する。
        - DomainやApplicationをDrizzleに依存させない。
    - 初期保存対象
        - Phase 1に必要なものだけを初期保存対象にする。
        - 初期保存対象
            - User。
            - Organization。
            - Membership。
            - Project。
            - PhotoAsset。
            - Storyboard。
            - Scene。
            - StylePreset。
            - GenerationRequest。
            - GeneratedImage。
        - User/Organizationは、WorkOSや将来認証に備えて最小形のみ持つ。
        - 後回しにする保存対象
            - GenerationVersion。
            - Coin/Credit履歴。
    - Storyboard / Scene設計
        - `storyboards` と `scenes` は別テーブルにする。
        - `storyboards` は、ストーリーボード全体の設定や構成単位を扱う。
        - `scenes` は、各シーンの順序、説明、プロンプト、生成対象を扱う。
        - シーン単位で生成、失敗、再生成、順序変更、写真紐づけ、生成画像比較を行うため、JSONだけで1テーブルにまとめない。
        - 初期段階では、`ScenePrompt` や `SceneTextBlock` のような細かいテーブル分割は不要とする。
        - `storyboards` の主な項目例
            - `id`。
            - `projectId`。
            - `title`。
            - `status`。
            - `stylePresetId`。
            - `tone`。
            - `createdAt`。
            - `updatedAt`。
            - `deletedAt`。
        - `scenes` の主な項目例
            - `id`。
            - `storyboardId`。
            - `orderIndex`。
            - `title`。
            - `description`。
            - `prompt`。
            - `status`。
            - `createdAt`。
            - `updatedAt`。
            - `deletedAt`。
    - GenerationRequest / GeneratedImage設計
        - `generation_requests` は、画像生成、将来の動画生成、将来のBGM生成を共通で扱うジョブテーブルとする。
        - 初期の生成結果テーブルは `generated_images` のみ作成する。
        - 将来的に動画生成やBGM生成に対応する場合は、`generated_videos` や `generated_bgms` を追加する。
        - 生成リクエストは共通化するが、生成結果テーブルは無理に共通化しない。
        - 生成結果を最初から1つの `generated_assets` に押し込まず、nullableな項目が増えすぎることを避ける。
        - `generation_requests` の主な項目例
            - `id`。
            - `projectId`。
            - `storyboardId`。
            - `sceneId`。
                - BGMなどシーンに紐づかない生成ではnullableにする。
            - `type`。
                - `image`。
                - `video`。
                - `bgm`。
            - `status`。
                - `queued`。
                - `running`。
                - `succeeded`。
                - `failed`。
                - `canceled`。
            - `provider`。
            - `model`。
            - `inputJson`。
            - `errorMessage`。
            - `startedAt`。
            - `completedAt`。
            - `createdAt`。
        - `generated_images` の主な項目例
            - `id`。
            - `generationRequestId`。
            - `projectId`。
            - `storyboardId`。
            - `sceneId`。
            - `storageKey`。
            - `width`。
            - `height`。
            - `mimeType`。
            - `seed`。
            - `isAdopted`。
            - `createdAt`。
            - `deletedAt`。
    - StylePreset設計
        - システムプリセットとユーザー作成カスタムスタイルは、同じ `style_presets` テーブルで扱う。
        - `scope` とowner情報で利用範囲を分ける。
        - `style_presets` の主な項目例
            - `id`。
            - `name`。
            - `description`。
            - `promptTemplate`。
            - `scope`。
                - `system`。
                - `organization`。
                - `user`。
            - `ownerUserId`。
            - `organizationId`。
            - `isActive`。
            - `createdAt`。
            - `updatedAt`。
        - `system` のStylePresetはユーザーが直接編集できないようにする。
        - システムプリセットをカスタム化する場合は、複製してユーザー用プリセットとして保存する。
            - `system preset`。
            - `duplicate`。
            - `user custom preset`。
    - PhotoAsset / ScenePhotoAsset設計
        - `PhotoAsset` には、写真自体の管理状態を持つ。
        - 実際の使用/参考は、`ScenePhotoAsset` でSceneとの関係として持つ。
        - 写真の使用状態は文脈依存であり、Photo単体だけで完全には決めない。
        - 同じ写真が、あるSceneでは主素材、別のSceneでは参考画像、別のStoryboardでは未使用になる可能性を考慮する。
        - `photo_assets` の主な項目例
            - `id`。
            - `projectId`。
            - `storageKey`。
            - `originalFileName`。
            - `mimeType`。
            - `width`。
            - `height`。
            - `curationStatus`。
                - `candidate`。
                - `excluded`。
                - `reference`。
            - `createdAt`。
            - `deletedAt`。
        - `scene_photo_assets` の主な項目例
            - `id`。
            - `sceneId`。
            - `photoAssetId`。
            - `role`。
                - `primary`。
                - `reference`。
            - `orderIndex`。
            - `createdAt`。
        - 1つのSceneには、複数のPhotoAssetを紐づけられるようにする。
        - 主素材1枚 + 参考複数枚を扱える設計にする。
        - `primary` は1シーンにつき最大1枚とする。
        - `reference` は1シーンに複数紐づけ可能とする。
        - DB制約で完全に守れる場合はDB制約を利用する。
        - SQLiteで部分ユニーク制約などの扱いが難しい場合は、RepositoryまたはApplication側で制約を保証する。
        - UI上の判定
            - 未使用は、`scene_photo_assets` に紐づいていない状態として扱う。
            - 使用は、`role: primary` で紐づいている状態として扱う。
            - 参考のみは、`role: reference` で紐づいている状態として扱う。
        - 写真管理画面でのざっくり分類には、`photo_assets.curationStatus` を使用してよい。
    - 採用画像
        - 採用画像は `generated_images.isAdopted` で管理する。
        - 1シーンにつき採用画像は原則1枚とする。
        - `sceneId` ごとに `isAdopted = true` は最大1件に制限する。
        - DB制約で完全に守れる場合はDB制約を利用する。
        - DB制約が扱いにくい場合は、Application側で保証する。
        - 採用切替はトランザクションで行う。
            - 同じ `sceneId` の既存採用画像を `false` にする。
            - 対象画像を `true` にする。
        - 将来的に1シーン複数カットや複数採用候補が必要になった場合は、`adoptionGroup` や `slot` の追加を検討する。
    - 画像ファイルの保存方針
        - 元写真や生成画像のバイナリ本体はDBに入れない。
        - 画像本体は、ファイルストレージまたは将来のオブジェクトストレージに保存する。
        - DBには画像メタデータとstorage keyを保存する。
        - DBに保存する画像メタデータ例
            - `storageKey`。
            - `mimeType`。
            - `size`。
            - `width`。
            - `height`。
            - `checksum`。
            - `sourceKind`。
            - `createdAt`。
        - ローカル保存先の例
            - `data/uploads/originals/...`
            - `data/uploads/generated/...`
        - 将来S3、R2、GCSなどに差し替えられるように、DBには絶対パスではなくstorage keyを保存する。
        - `photo_assets.storageKey` や `generated_images.storageKey` のような形で管理する。
    - 削除/復元
        - 削除はsoft deleteを基本にする。
        - DB上では `deletedAt` を持つ方針とする。
        - 削除後7日以内は復元できるようにする。
        - 復元期間を過ぎたデータは完全削除対象にする。
    - 状態管理
        - 状態は1箇所にすべて押し込まず、責務ごとに分けて持つ。
        - `projects.status` は、プロジェクト全体のライフサイクルを扱う。
            - `draft`。
            - `active`。
            - `completed`。
            - `archived`。
        - `storyboards.status` は、ストーリーボード編集状態を扱う。
            - `draft`。
            - `editing`。
            - `ready`。
            - `completed`。
        - `scenes.status` は、シーン単位の編集/生成準備状態を扱う。
            - `draft`。
            - `ready`。
            - `completed`。
        - `generation_requests.status` は、生成ジョブの実行状態を扱う。
            - `queued`。
            - `running`。
            - `succeeded`。
            - `failed`。
            - `canceled`。
        - 生成失敗は、原則として `generation_requests.status` に閉じる。
        - `storyboards.status` に生成失敗を直接持たせない。
        - UIでプロジェクトやストーリーボードの生成失敗を表示する場合は、最新の `generation_requests.status` から計算する。
        - 必要になった場合のみ、将来的に `projects.lastGenerationStatus` のような表示用キャッシュ列を検討する。
        - 初期段階では、同じ意味の状態を複数箇所で二重管理しない。
    - GenerationRequestの入力保存
        - `generation_requests.inputJson` には、生成入力の詳細を丸ごと保存する。
        - 再現性、デバッグ、将来の比較、監査のために保存する。
        - `inputJson` に保存する項目例
            - `prompt`。
            - `negativePrompt`。
            - `model`。
            - `provider`。
            - `size`。
            - `quality`。
            - `format`。
            - `seed`。
            - `stylePresetId`。
            - `sceneId`。
            - `referencePhotoAssetIds`。
            - `inputGeneratedImageIds`。
            - `parameters`。
        - 検索、一覧、フィルター、状態管理に使う項目は別カラムにも持つ。
            - `type`。
            - `status`。
            - `provider`。
            - `model`。
            - `storyboardId`。
            - `sceneId`。
        - 検索や状態管理に使うものはカラムで扱う。
        - 再現、監査、詳細設定に使うものは `inputJson` で扱う。
    - DBマイグレーション
        - DBマイグレーションはDrizzle Kitで管理する。
        - schema定義はTypeScriptで行う。
        - migration生成はDrizzle Kitで行う。
        - migration適用はDrizzle Kitまたはcustom scriptで行う。
        - ローカルSQLiteでも初期からマイグレーションを残す。
        - 推奨配置例
            - `packages/db` または `apps/api/src/db/schema.ts`。
            - `drizzle/migrations/`。
        - Domain層はDrizzle schemaをimportしない。
        - Drizzle schemaはInfrastructure/Adapter側に閉じる。
    - 初期DBテスト方針
        - Repository単位でSQLiteに対して確認する。
        - Storyboardを削除した場合、Sceneが通常一覧から消えることを確認する。
        - Sceneの `orderIndex` で順序が再現できることを確認する。
        - GenerationRequest失敗時に、Storyboard自体がfailedにならないことを確認する。
        - PhotoAssetの未使用/使用/参考のみが正しく判定されることを確認する。
        - system StylePresetを直接編集できないことを確認する。
    - 将来差し替え
        - 初期はSQLite + Drizzleで実装する。
        - DBアクセスはRepository経由に留める。
        - 将来的にPostgreSQLなどへ移行しやすい設計にする。
    - 完了条件
        - ローカルDB方針を記録する。
        - 本番DB候補と未確定方針を記録する。
        - Drizzle採用方針を記録する。
        - 初期保存対象を記録する。
        - Storyboard/Scene分離方針を記録する。
        - GenerationRequest/GeneratedImage設計を記録する。
        - StylePreset設計を記録する。
        - PhotoAsset/ScenePhotoAsset設計を記録する。
        - 採用画像の管理方針を記録する。
        - 画像バイナリとメタデータの保存方針を記録する。
        - soft deleteと7日復元方針を記録する。
        - 状態管理方針を記録する。
        - GenerationRequestの入力保存方針を記録する。
        - Drizzle Kitによるマイグレーション方針を記録する。
