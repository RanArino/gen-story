## ユーザー体験の流れ

### フェーズ1: ストーリーボード作成

1. プロジェクトを作成する。
2. 写真をアップロードする。
3. 写真ごとにメモ・利用状態を設定する。
    - 使用。
    - 未使用。
    - 参考のみ。
4. 写真順をユーザーが調整する。
5. 必要ならAIおすすめ順を使う。
6. 感情候補・シーン構成をAIが提案する。
7. ユーザーが感情・構成を調整する。
8. プロジェクト共通プロンプトと画像スタイルを決める。
9. 画像生成モデルを選択する。
    - 初期のデフォルトはOpenAI `gpt-image-2` とする。
    - モデル選択UIでは、`gpt-image-2` のような実モデル名も表示する。
    - 初期実装ではOpenAIのみを対象とする。
    - 将来的に、`SeedanceImageAdapter` や別プロバイダーを追加できるようにする。
    - 将来的に複数プロバイダーを扱う場合は、プロバイダーごとにグループ分けして実モデルを表示する。
10. テスト画像を3パターン生成する。
11. ユーザーが1枚を選んでスタイルを確定する。
12. 全シーン画像を一括生成する。
    - 一括生成時に、画像サイズ・品質・形式を初心者向けプリセットから指定できるようにする。
    - 初期プリセットは「標準」とする。
    - 将来的には、生成前に必要な金額または専用コイン消費量の見積もりを表示する。
13. ストーリーボードをカード / タイムライン / 一覧表で確認する。
14. シーンごとに編集・再生成・採用判断を行う。
15. 必要ならシーン間のプラス操作でAI補完シーンを追加する。
16. 作業状態をDBに常時保存する。
17. 生成結果をユーザーがいつでも確認できるように保存する。
18. 画像生成のバージョン履歴とリクエスト内容を、生成履歴として簡易表示できるように保存する。
19. 元写真や生成画像を削除した場合は、基本的に7日以内の復元期間を設ける。
20. JSON出力・フェーズ2用構造化データとして保存する。

### フェーズ2: 動画生成

1. フェーズ1で完成したストーリーボードと採用画像セットを読み込む。
2. ストーリーボードをもとに、動画生成プロンプトを作成する。
3. 採用画像から、シーンごとの短い動画クリップを生成する。
    - 初期値は1シーンあたり5秒程度とする。
    - シーンごとに尺を変更できるようにする。
    - 指定可能な尺の範囲は、動画生成モデルの仕様に従う。
4. 写真枚数とシーン数から、1本の動画全体の尺を自動計算する。
5. 必要に応じて、シーンごとの動画を再生成する。
6. BGMのみ生成する。
    - BGM生成にはSuno AIを使用する想定とする。
    - 初期は歌詞なしのBGMのみとする。
    - BGMは、基本的に動画全体に対する1本として扱う。
    - 将来的には歌ありも検討し、ユーザーが選択可能にする。
7. CapCutなどに渡せる素材一式を作成する。
    - 生成動画クリップ。
    - 採用画像/採用動画。
    - 動画全体のBGM。
    - 字幕テキスト。
        - 字幕は将来対応とする。
    - 編集メモ。
8. 必要に応じて、簡単なトランジションを選択する。
    - OSSや既存ライブラリで簡単に実現できる場合のみ対応する。
    - CanvaやCapCutのような高度な編集機能とは競争しない。
9. 動画生成リクエスト履歴を保存する。
    - 使用モデル。
    - 動画プロンプト。
    - 元画像ID。
    - シーンID。
    - 尺。
    - 成功/失敗。
    - エラー理由。
    - 生成日時。
    - コスト/コイン消費量。
10. アプリ内でMP4形式の1本の動画として書き出す。

### フェーズ3: AIエディタ連携

1. CodexやClaude Code EditorなどのAIエディタから、構造化されたストーリーボードを読み込む。
2. ユーザーがチャットでストーリーボード修正を指示する。
3. AIエディタが、シーン説明、プロンプト、画像生成設定、動画生成設定を修正する。
4. 初期は、ストーリーボードJSONの修正、画像生成プロンプトの一括調整、フェーズ2用動画プロンプト生成を自動化対象にする。
5. 必要に応じて、画像生成や文字生成をユーザーのCodexプランで実行できるようにする。
6. `USER_EXPERIENCE_FLOW.md` のUI操作を、将来的にはチャット内で完結できるようにする。
7. 初期段階では、一般ユーザー向けではなく、開発者がローカルで使う機能として扱う。
